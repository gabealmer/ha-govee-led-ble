# H6199 DIY effect uploaded over 0xA3, captured 2026-08-04 from the Govee Home iOS app
# driving the DreamView T1. Jumping2. Its variant is 2, not 1, which is half the evidence that the variant is an identifier and not an index.
id: h6199_effect_diy_jumping2
data: h6199_effect_diy_jumping2.bin
imports:
  - h6199_effect_upload
asserts:
  - actual: chunk_count
    expected: 2
  - actual: kind
    expected: 4
  - actual: content.family
    expected: 1
  - actual: content.variant
    expected: 2
  - actual: content.speed
    expected: 92
  - actual: content.palette_len
    expected: 21
  - actual: content.palette.size
    expected: 7
  - actual: content.palette[0].red
    expected: 255
  - actual: content.palette[0].green
    expected: 0
  - actual: content.palette[3].green
    expected: 255
  - actual: content.padding
    expected: "000000000000"
