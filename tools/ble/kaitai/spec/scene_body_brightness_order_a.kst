# Effects Lab Brightness Order A, captured live 2026-07-26 on the user-authored scene "Hey There" (activation 33 05 04 92 01, code 402). Apply-before, every other control untouched.
id: scene_body_brightness_order_a
data: scene_body_brightness_order_a.bin
imports:
  - scene_body
asserts:
  - actual: scene_type
    expected: 2
  - actual: record_count
    expected: 1
  - actual: records.size
    expected: 1
  - actual: padding.size
    expected: 0
  - actual: records[0].rec_len
    expected: 29
  - actual: records[0].body.select_type
    expected: 1
  - actual: records[0].body.colour_count
    expected: 2
  - actual: records[0].body.excess.size
    expected: 0
  - actual: records[0].body.brightness_blocks[0].brightness_order
    expected: 0
