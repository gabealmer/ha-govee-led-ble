# The aa 40 query. Same all-zero-body trap as the brightness pair above.
id: status_reply_unit_count_query
data: status_reply_unit_count_query.bin
imports:
  - status_reply
asserts:
  - actual: domain
    expected: 64
  - actual: body.reserved
    expected: 0
  - actual: body.count
    expected: 0
